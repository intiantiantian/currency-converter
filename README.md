# Currency Converter Tooltip

A Chrome extension (Manifest V3) that detects highlighted currency amounts on web pages and displays an instant conversion tooltip based on custom exchange rate preferences.

## Features
- Hover conversion on highlighted text
- Local storage for custom target currency
- Manual "Update Rates" button using public API
